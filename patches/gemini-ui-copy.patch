--- a/src/aiassistant/aiassistantwidget.cpp
+++ b/src/aiassistant/aiassistantwidget.cpp
@@ -308 +308 @@ AiAssistantWidget::AiAssistantWidget(QWidget *parent)
-        i18n("One open tool layer for every agent. Connect an OpenAI-compatible API inside the editor, or let external agents control Update P5 Edit Aja through MCP."), this);
+        i18n("Built-in AI Agent uses Google Gemini only. Add up to 100 Gemini API keys in the API section; editor tools remain available through the same native tool layer."), this);
@@ -312 +312 @@ AiAssistantWidget::AiAssistantWidget(QWidget *parent)
-    auto *agentGroup = new QGroupBox(i18n("AI Agent — Main Control"), this);
+    auto *agentGroup = new QGroupBox(this);
