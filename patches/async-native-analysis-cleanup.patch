--- a/src/mainwindow.cpp
+++ b/src/mainwindow.cpp
@@ -126,7 +126,8 @@
 #include <QDir>
 #include <QDialogButtonBox>
 #include <QDomDocument>
-#include <QElapsedTimer>
+#include <QPointer>
+#include <QTimer>
 #include <QFile>
 #include <QFileDialog>
 #include <QFileInfo>
@@ -164,6 +165,25 @@
 KIO::filesize_t m_totalCacheSize = 0;
 int m_totalCacheJobs = 0;
 
+static void stopAgentOwnedProcess(QProcess *process)
+{
+    if (!process || process->state() == QProcess::NotRunning) {
+        return;
+    }
+    const qint64 pid = process->processId();
+#ifdef Q_OS_WIN
+    if (pid > 0) {
+        QProcess::startDetached(QStringLiteral("taskkill"),
+                                QStringList{QStringLiteral("/PID"), QString::number(pid), QStringLiteral("/T"), QStringLiteral("/F")});
+    }
+#else
+    if (pid > 0) {
+        QProcess::startDetached(QStringLiteral("pkill"), QStringList{QStringLiteral("-TERM"), QStringLiteral("-P"), QString::number(pid)});
+    }
+#endif
+    process->kill();
+}
+
 MainWindow::MainWindow(QWidget *parent)
     : KXmlGuiWindow(parent)
     , m_activeTool(ToolType::SelectTool)
@@ -2158,254 +2178,6 @@
                                    {QStringLiteral("agent_image_path"), output.absoluteFilePath()}};
             }
 
-            if (toolName == QLatin1String("kdenlive_detect_silence")) {
-                QString sourcePath = arguments.value(QStringLiteral("path")).toString().trimmed();
-                QString binId = arguments.value(QStringLiteral("bin_id")).toString().trimmed();
-                const int clipId = arguments.value(QStringLiteral("clip_id")).toInt(-1);
-                if (binId.isEmpty() && model->isClip(clipId)) {
-                    binId = model->getClipBinId(clipId);
-                }
-                if (sourcePath.isEmpty() && !binId.isEmpty()) {
-                    const auto projectModel = pCore->projectItemModel();
-                    const auto clip = projectModel ? projectModel->getClipByBinID(binId) : nullptr;
-                    if (clip) {
-                        sourcePath = clip->url();
-                    }
-                }
-                if (sourcePath.startsWith(QLatin1String("file://"))) {
-                    sourcePath = QUrl(sourcePath).toLocalFile();
-                }
-                if (sourcePath.isEmpty() || !QFileInfo::exists(sourcePath)) {
-                    return errorResult(QStringLiteral("Provide an existing media path, bin_id, or timeline clip_id"));
-                }
-
-                QString ffmpeg = arguments.value(QStringLiteral("ffmpeg_path")).toString().trimmed();
-                if (ffmpeg.isEmpty()) {
-                    ffmpeg = QStandardPaths::findExecutable(QStringLiteral("ffmpeg"));
-                }
-                if (ffmpeg.isEmpty() || !QFileInfo::exists(ffmpeg)) {
-                    return errorResult(QStringLiteral("FFmpeg executable was not found. Install FFmpeg or provide ffmpeg_path."));
-                }
-                const double noiseDb = qBound(-100.0, arguments.value(QStringLiteral("noise_db")).toDouble(-35.0), 0.0);
-                const double minimum = qBound(0.05, arguments.value(QStringLiteral("minimum_silence_seconds")).toDouble(0.45), 30.0);
-                const int timeoutSeconds = qBound(5, arguments.value(QStringLiteral("timeout_seconds")).toInt(120), 600);
-                const QString filter = QStringLiteral("silencedetect=noise=%1dB:d=%2").arg(noiseDb, 0, 'f', 1).arg(minimum, 0, 'f', 3);
-
-                QProcess process;
-                process.setProgram(ffmpeg);
-                process.setArguments({QStringLiteral("-hide_banner"), QStringLiteral("-nostats"), QStringLiteral("-i"), sourcePath,
-                                      QStringLiteral("-vn"), QStringLiteral("-af"), filter, QStringLiteral("-f"), QStringLiteral("null"), QStringLiteral("-")});
-                process.start();
-                if (!process.waitForStarted(5000)) {
-                    return errorResult(QStringLiteral("Could not start FFmpeg: %1").arg(process.errorString()));
-                }
-                if (!process.waitForFinished(timeoutSeconds * 1000)) {
-                    process.kill();
-                    process.waitForFinished(2000);
-                    return errorResult(QStringLiteral("FFmpeg silence detection timed out after %1 seconds").arg(timeoutSeconds));
-                }
-                const QString diagnostic = QString::fromUtf8(process.readAllStandardError());
-                if (process.exitStatus() != QProcess::NormalExit || process.exitCode() != 0) {
-                    return errorResult(QStringLiteral("FFmpeg failed: %1").arg(diagnostic.right(3000)));
-                }
-
-                const QRegularExpression startRe(QStringLiteral("silence_start:\\s*([+-]?(?:[0-9]+(?:\\.[0-9]*)?|\\.[0-9]+))"));
-                const QRegularExpression endRe(QStringLiteral("silence_end:\\s*([+-]?(?:[0-9]+(?:\\.[0-9]*)?|\\.[0-9]+)).*silence_duration:\\s*([0-9.]+)"));
-                QJsonArray silences;
-                double pendingStart = -1.0;
-                const QStringList lines = diagnostic.split(QLatin1Char('\n'));
-                for (const QString &line : lines) {
-                    const auto startMatch = startRe.match(line);
-                    if (startMatch.hasMatch()) {
-                        pendingStart = startMatch.captured(1).toDouble();
-                    }
-                    const auto endMatch = endRe.match(line);
-                    if (endMatch.hasMatch()) {
-                        const double end = endMatch.captured(1).toDouble();
-                        const double duration = endMatch.captured(2).toDouble();
-                        const double start = pendingStart >= 0.0 ? pendingStart : qMax(0.0, end - duration);
-                        silences.push_back(QJsonObject{{QStringLiteral("start_seconds"), start},
-                                                       {QStringLiteral("end_seconds"), end},
-                                                       {QStringLiteral("duration_seconds"), duration}});
-                        pendingStart = -1.0;
-                    }
-                }
-                return QJsonObject{{QStringLiteral("ok"), true},
-                                   {QStringLiteral("operation"), toolName},
-                                   {QStringLiteral("path"), QFileInfo(sourcePath).absoluteFilePath()},
-                                   {QStringLiteral("noise_db"), noiseDb},
-                                   {QStringLiteral("minimum_silence_seconds"), minimum},
-                                   {QStringLiteral("count"), silences.size()},
-                                   {QStringLiteral("silences"), silences}};
-            }
-
-            if (toolName == QLatin1String("kdenlive_transcribe_media")) {
-                QString sourcePath = arguments.value(QStringLiteral("path")).toString().trimmed();
-                QString binId = arguments.value(QStringLiteral("bin_id")).toString().trimmed();
-                const int clipId = arguments.value(QStringLiteral("clip_id")).toInt(-1);
-                if (binId.isEmpty() && model->isClip(clipId)) {
-                    binId = model->getClipBinId(clipId);
-                }
-                if (sourcePath.isEmpty() && !binId.isEmpty()) {
-                    const auto projectModel = pCore->projectItemModel();
-                    const auto clip = projectModel ? projectModel->getClipByBinID(binId) : nullptr;
-                    if (clip) {
-                        sourcePath = clip->url();
-                    }
-                }
-                if (sourcePath.startsWith(QLatin1String("file://"))) {
-                    sourcePath = QUrl(sourcePath).toLocalFile();
-                }
-                if (sourcePath.isEmpty() || !QFileInfo::exists(sourcePath)) {
-                    return errorResult(QStringLiteral("Provide an existing media path, bin_id, or timeline clip_id"));
-                }
-
-                SpeechToTextWhisper stt(this);
-                const QString script = stt.speechScript();
-                const auto pythonExec = stt.venvPythonExecs();
-                if (script.isEmpty() || !QFileInfo::exists(script)) {
-                    return errorResult(QStringLiteral("Kdenlive Whisper script is unavailable. Configure Speech Recognition in Kdenlive first."));
-                }
-                if (pythonExec.python.isEmpty()) {
-                    return errorResult(QStringLiteral("Kdenlive Whisper Python environment is unavailable. Configure Speech Recognition in Kdenlive first."));
-                }
-
-                const QString modelName = arguments.value(QStringLiteral("model")).toString(KdenliveSettings::whisperModel()).trimmed();
-                if (modelName.isEmpty()) {
-                    return errorResult(QStringLiteral("No Whisper model is configured"));
-                }
-                QString device = arguments.value(QStringLiteral("device")).toString(KdenliveSettings::whisperDevice()).trimmed();
-                if (device.isEmpty()) {
-                    device = QStringLiteral("cpu");
-                }
-                QString language = arguments.value(QStringLiteral("language")).toString().trimmed();
-                if (language.isEmpty()) {
-                    language = KdenliveSettings::whisperLanguage().trimmed();
-                }
-                const QString task = arguments.value(QStringLiteral("task")).toString(QStringLiteral("transcribe")) == QLatin1String("translate")
-                                         ? QStringLiteral("translate")
-                                         : QStringLiteral("transcribe");
-                QString ffmpeg = arguments.value(QStringLiteral("ffmpeg_path")).toString().trimmed();
-                if (ffmpeg.isEmpty()) {
-                    ffmpeg = KdenliveSettings::ffmpegpath();
-                }
-                if (ffmpeg.isEmpty() || !QFileInfo::exists(ffmpeg)) {
-                    ffmpeg = QStandardPaths::findExecutable(QStringLiteral("ffmpeg"));
-                }
-                if (ffmpeg.isEmpty()) {
-                    return errorResult(QStringLiteral("FFmpeg executable was not found"));
-                }
-                const int timeoutSeconds = qBound(30, arguments.value(QStringLiteral("timeout_seconds")).toInt(1800), 7200);
-
-                QStringList processArgs{script, QStringLiteral("-S"), sourcePath, QStringLiteral("-M"), modelName, QStringLiteral("-D"), device,
-                                        QStringLiteral("-T"), task, QStringLiteral("-F"), ffmpeg};
-                if (!language.isEmpty()) {
-                    processArgs << QStringLiteral("-L") << language;
-                }
-
-                QProcess process;
-                process.setProcessChannelMode(QProcess::MergedChannels);
-                process.setProgram(pythonExec.python);
-                process.setArguments(processArgs);
-                process.start();
-                if (!process.waitForStarted(10000)) {
-                    return errorResult(QStringLiteral("Could not start Whisper: %1").arg(process.errorString()));
-                }
-                QElapsedTimer timer;
-                timer.start();
-                QByteArray outputBytes;
-                while (process.state() != QProcess::NotRunning) {
-                    process.waitForReadyRead(500);
-                    outputBytes += process.readAll();
-                    if (timer.elapsed() > timeoutSeconds * 1000LL) {
-                        process.kill();
-                        process.waitForFinished(3000);
-                        outputBytes += process.readAll();
-                        return errorResult(QStringLiteral("Whisper transcription timed out after %1 seconds").arg(timeoutSeconds));
-                    }
-                }
-                outputBytes += process.readAll();
-                if (process.exitStatus() != QProcess::NormalExit || process.exitCode() != 0) {
-                    return errorResult(QStringLiteral("Whisper failed: %1").arg(QString::fromUtf8(outputBytes.right(5000))));
-                }
-
-                const QString transcriptOutput = QString::fromUtf8(outputBytes);
-                const QRegularExpression headerRe(QStringLiteral("^\\[([0-9.+-]+)>([0-9.+-]+)\\]\\s*$"));
-                const QRegularExpression wordRe(QStringLiteral("^\\[([0-9.+-]+)>([0-9.+-]+)\\](.*)$"));
-                QJsonArray segments;
-                double currentStart = -1.0;
-                double currentEnd = -1.0;
-                QString currentText;
-                auto flushSegment = [&segments, &currentStart, &currentEnd, &currentText]() {
-                    const QString text = currentText.simplified();
-                    if (currentStart >= 0.0 && currentEnd > currentStart && !text.isEmpty()) {
-                        segments.push_back(QJsonObject{{QStringLiteral("start_seconds"), currentStart},
-                                                       {QStringLiteral("end_seconds"), currentEnd},
-                                                       {QStringLiteral("text"), text}});
-                    }
-                    currentStart = -1.0;
-                    currentEnd = -1.0;
-                    currentText.clear();
-                };
-                for (const QString &line : transcriptOutput.split(QLatin1Char('\n'))) {
-                    const auto headerMatch = headerRe.match(line.trimmed());
-                    if (headerMatch.hasMatch()) {
-                        flushSegment();
-                        currentStart = headerMatch.captured(1).toDouble();
-                        currentEnd = headerMatch.captured(2).toDouble();
-                        continue;
-                    }
-                    const auto wordMatch = wordRe.match(line);
-                    if (wordMatch.hasMatch() && currentStart >= 0.0) {
-                        currentText += wordMatch.captured(3);
-                    }
-                }
-                flushSegment();
-                if (segments.isEmpty()) {
-                    return errorResult(QStringLiteral("Whisper completed but no timestamped segments were parsed. Output tail: %1").arg(transcriptOutput.right(3000)));
-                }
-
-                QJsonArray timelineSegments;
-                if (model->isClip(clipId)) {
-                    const auto inOut = model->getClipInOut(clipId);
-                    const int clipStart = model->getItemPosition(clipId);
-                    const int clipEnd = model->getItemEnd(clipId);
-                    const double speed = qAbs(model->getClipSpeed(clipId));
-                    if (speed > 0.000001) {
-                        for (const QJsonValue &value : segments) {
-                            const QJsonObject segment = value.toObject();
-                            const int sourceStart = qRound(segment.value(QStringLiteral("start_seconds")).toDouble() * fps);
-                            const int sourceEnd = qRound(segment.value(QStringLiteral("end_seconds")).toDouble() * fps);
-                            const int overlapStart = qMax(sourceStart, inOut.first);
-                            const int overlapEnd = qMin(sourceEnd, inOut.second + 1);
-                            if (overlapEnd <= overlapStart) {
-                                continue;
-                            }
-                            const int timelineStart = qBound(clipStart, clipStart + qRound((overlapStart - inOut.first) / speed), clipEnd);
-                            const int timelineEnd = qBound(clipStart, clipStart + qRound((overlapEnd - inOut.first) / speed), clipEnd);
-                            if (timelineEnd > timelineStart) {
-                                timelineSegments.push_back(QJsonObject{{QStringLiteral("start_frame"), timelineStart},
-                                                                       {QStringLiteral("end_frame"), timelineEnd},
-                                                                       {QStringLiteral("start_seconds"), timelineStart / fps},
-                                                                       {QStringLiteral("end_seconds"), timelineEnd / fps},
-                                                                       {QStringLiteral("text"), segment.value(QStringLiteral("text"))}});
-                            }
-                        }
-                    }
-                }
-
-                return QJsonObject{{QStringLiteral("ok"), true},
-                                   {QStringLiteral("operation"), toolName},
-                                   {QStringLiteral("path"), QFileInfo(sourcePath).absoluteFilePath()},
-                                   {QStringLiteral("model"), modelName},
-                                   {QStringLiteral("device"), device},
-                                   {QStringLiteral("task"), task},
-                                   {QStringLiteral("language"), language},
-                                   {QStringLiteral("count"), segments.size()},
-                                   {QStringLiteral("segments"), segments},
-                                   {QStringLiteral("timeline_segments"), timelineSegments}};
-            }
-
             if (toolName == QLatin1String("kdenlive_remove_ranges")) {
                 const QVector<int> tracks = resolveTracks(arguments.value(QStringLiteral("track_ids")).toArray());
                 QJsonArray normalized;
