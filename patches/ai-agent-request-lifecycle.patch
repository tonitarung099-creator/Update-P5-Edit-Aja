--- a/src/aiassistant/openaicompatibleagent.cpp
+++ b/src/aiassistant/openaicompatibleagent.cpp
@@ -33,7 +33,8 @@
 void OpenAiCompatibleAgent::run(const QString &prompt, bool fullToolAccess, int maxRounds, bool attachToolImages)
 {
     if (m_reply) {
-        fail(QStringLiteral("An agent request is already running."));
+        // Reject only the extra request; the active request still owns busy state.
+        Q_EMIT failed(QStringLiteral("An agent request is already running."));
         return;
     }
     if (m_url.isEmpty() || m_model.isEmpty()) {
@@ -65,9 +66,12 @@
 
 void OpenAiCompatibleAgent::cancel()
 {
-    if (m_reply) {
-        m_reply->abort();
-        m_reply.clear();
+    // abort() may emit finished synchronously. Detach first so its callback
+    // cannot report a cancellation as an API error or alter a later run.
+    QNetworkReply *reply = m_reply.data();
+    m_reply.clear();
+    if (reply) {
+        reply->abort();
     }
     Q_EMIT busyChanged(false);
 }
@@ -96,9 +100,11 @@
     QNetworkReply *reply = m_network->post(request, QJsonDocument(payload).toJson(QJsonDocument::Compact));
     m_reply = reply;
     connect(reply, &QNetworkReply::finished, this, [this, reply]() {
-        if (m_reply == reply) {
-            m_reply.clear();
+        if (m_reply != reply) {
+            reply->deleteLater();
+            return;
         }
+        m_reply.clear();
         handleReply(reply);
         reply->deleteLater();
     });
--- a/src/aiassistant/aiassistantwidget.cpp
+++ b/src/aiassistant/aiassistantwidget.cpp
@@ -1536,6 +1536,11 @@
 
 void AiAssistantWidget::runAgent()
 {
+    // Return/shortcut or programmatic activation must not clear the current
+    // trace, replace API settings, or create a second checkpoint while busy.
+    if (!m_runButton->isEnabled()) {
+        return;
+    }
     const QString prompt = m_prompt->text().trimmed();
     if (prompt.isEmpty()) {
         appendTrace(i18n("Type an editing request first."));
@@ -2383,6 +2388,7 @@
 void AiAssistantWidget::setBusy(bool busy)
 {
     m_runButton->setEnabled(!busy);
+    m_prompt->setEnabled(!busy);
     m_cancelButton->setEnabled(busy);
     m_runButton->setText(busy ? i18n("Agent working…") : i18n("Run Agent"));
 }
